# grafana-dashboard.json
{
  "dashboard": {
    "panels": [
      {
        "type": "graph",
        "title": "CPU Usage",
        "targets": [
          {
            "expr": "sum(rate(node_cpu_seconds_total{mode='idle'}[5m])) by (instance)",
            "interval": ""
          }
        ]
      }
    ]
  }
}

